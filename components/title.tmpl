<a href="/" class="title link">
  <header className="title header">
    R<span>une</span>
    <span className="title alt">
      M<span>arkers</span>
    </span>
    <span>.net</span>
  </header>
</a>

<style>
.title.header {
  font-weight: bold;
  padding: 0 20px;
  display: flex;
  color: var(--color-text);
  text-decoration: none;
  font-size: 2rem;
}

.title.link {
  text-decoration: none;
}

.title.alt {
  color: var(--color-primary);
  display: flex;
}

.title span:not(.titleAlt),
.title.alt span {
  display: none;
}

@media screen and (min-width: 768px) {
  .title.header span:not(.titleAlt),
  .title.alt span {
    display: flex;
  }
}
</style>
