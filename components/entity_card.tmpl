<h1>{{ .Name }}</h1>
<p>{{ .Subcategory }}</p>
<p>{{ .Wiki }}</p>
<button class="entity btn-copy">Click me</button>

<style>
  .entity.btn-copy{
    display: block;
    padding: .75rem;
    background: var(--color-primary);
    border: none;
    border-radius: .75rem;
    color: var(--color-background);
    font-size: 1.2rem;
    text-decoration: none;
    cursor: pointer;
    transition: all 0.2s ease-out;
    height: 2.5rem;
    overflow: hidden;
    box-shadow: 0 0.2rem 0.4rem rgba(0, 0, 0, 0.16), 0 0.2rem 0.4rem rgba(0, 0, 0, 0.23);
  }

  .entity.btn-copy:hover {
    background: var(--color-primary-light);
    transform: translateY(-0.05rem);
    box-shadow: 0 0.65rem 1.25rem rgba(0, 0, 0, 0.19), 0 0.4rem 0.4rem rgba(0, 0, 0, 0.23);
    color: black;
  }
</style>
