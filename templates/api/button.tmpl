<button
  class="copy-button animate"
  onclick="copy(this);
    this.classList.remove('animate');
    window.setTimeout(() => this.classList.add('animate'), 1);"
  data-tiles="{{ .TilesString }}"
>Copy</button>
