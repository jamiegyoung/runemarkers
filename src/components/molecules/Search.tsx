import Input from '@/components/atoms/Input';

export default function Search() {
  return (
    <div
      style={{
        display: `flex`,
        alignItems: `center`,
        marginBottom: 20,
        width: `100%`,
        justifyContent: `center`,
      }}
    >
      <Input />
    </div>
  );
}
